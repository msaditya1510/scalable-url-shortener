name: Keep Url Shortener Space Awake

on:
  schedule:
    # Runs every 6 hours (at minute 0)
     - cron: '0 */6 * * *'
  workflow_dispatch: # Allows you to run it manually for testing

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Space URL
        run: |
          curl -I https://msaditya1510-url-shortener.hf.space